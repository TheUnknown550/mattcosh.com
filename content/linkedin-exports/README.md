# LinkedIn Exports

Raw LinkedIn data export (`Profile.csv`, `Recommendations_Given.csv`,
`Recommendations_Received.csv`), used as source material for real content
on the site — see `src/data/projects.ts` and `src/components/home/*`.

These files are not read at runtime; they're kept here for reference so the
provenance of bio/project/testimonial copy is traceable.
